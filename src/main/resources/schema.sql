CREATE TABLE RECIPE (
     id INT AUTO_INCREMENT PRIMARY KEY,
     name VARCHAR(200),
     directions VARCHAR(1000),
     time VARCHAR(20),
     ingredients VARCHAR(300),
     category VARCHAR(30),
     img VARCHAR(100000)
);

